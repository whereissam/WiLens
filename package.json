{
  "name": "wilens",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "@zxing/browser": "^0.1.5"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vite": "^7.1.3",
    "vitest": "^4.1.9"
  }
}

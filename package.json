{
  "name": "wilens",
  "private": true,
  "version": "0.1.2",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "release:win": "cargo tauri build --runner cargo-xwin --target x86_64-pc-windows-msvc --config '{\"bundle\":{\"targets\":[\"nsis\"]}}' && cp \"$(ls -t src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/WiLens_*_x64-setup.exe | head -1)\" src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/WiLens-setup.exe && echo 'Built src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/WiLens-setup.exe'",
    "release:dmg": "cargo tauri build && cp \"$(ls -t src-tauri/target/release/bundle/dmg/WiLens_*_aarch64.dmg | head -1)\" src-tauri/target/release/bundle/dmg/WiLens.dmg && echo 'Built src-tauri/target/release/bundle/dmg/WiLens.dmg'"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@zxing/browser": "^0.2.1"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.1",
    "vitest": "^5.0.1"
  },
  "overrides": {
    "vite": "^8",
    "esbuild": ">=0.28.1",
    "picomatch": ">=4.0.4"
  }
}
